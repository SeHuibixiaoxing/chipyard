# 反馈清单（v3）

## 1) 增量策略

- [ ] 这种“`v2` 保持不动，`v3` 单独增量补充”的方式合适。
- [ ] `v3` 没有和 `v2` 重复太多内容。

备注：

## 2) pipeline buffer 细化

- [ ] 各类 buffer 的角色解释更清楚了。
- [ ] `DRAM/DRAM_DEPEN`、`ISOLATE/SHARED`、`ALL_RINGBUFFER` 的差别说明到位。
- [ ] “页从哪里来、命令怎么发、状态怎么推进”这三点解释到位。

备注：

## 3) 容量计算差异

- [ ] 开启松耦合时的容量公式解释准确。
- [ ] 不开启松耦合时的容量公式解释准确。
- [ ] 当前实现里 `shared/ringbuffer` 没进逐 stage penalty 这一点说明足够醒目。

备注：

## 4) 额外风险

- [ ] 新增风险项有价值。
- [ ] 风险描述仍然保持“问题-影响-建议”清晰结构。

备注：

## 5) 下一轮建议

- [ ] 需要把 `v3` 的增量内容正式合并回一版完整主文档。
- [ ] 需要继续细化 runtime 页表建立过程。
- [ ] 需要继续细化 scheduler 的资源调参逻辑。

备注：
